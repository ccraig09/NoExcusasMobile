class Eval {
  constructor(id, ownerId, title, date, notes) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.date = date;
    this.notes = notes;
  }
}

export default Eval;
