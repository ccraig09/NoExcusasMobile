class Eval {
  constructor(id, title, ownerId, docTitle, time) {
    this.id = id;
    this.title = title;
    this.ownerId = ownerId;
    this.docTitle = docTitle;
    this.time = time;
  }
}

export default Eval;
